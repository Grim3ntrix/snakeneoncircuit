---
description: Run the mandatory refinement pass on recent user-facing work
argument-hint: [file, feature, or area to refine]
---

Run the `polish-pass` skill against: $ARGUMENTS

If no target is given, scope it to the user-facing work in the current diff — check `git status` and `git diff` first.

Follow the skill as written, including the subtract-first step. Report what you changed, what you removed, and what you left alone and why.
